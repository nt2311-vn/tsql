# 2026-05-03 Journal

## Context

Started the `0.1.0` MVP after the CI/CD scaffold was merged.

## Decisions

- Use a hybrid CLI/TUI MVP.
- Support SQLite and Postgres first.
- Support TOML named connections plus direct `--url` override.
- Execute multi-statement SQL scripts instead of single statements only.
- Use Docker Compose locally and a Postgres service in CI for integration tests.
- Keep Snyk skipped for Cargo because Snyk CLI does not support Rust dependency scanning.

## Progress

- Created branch `feat/postgres-sqlite-0-1-0` from `main`.
- Added config loading and environment-variable expansion in `tsql-core`.
- Added paste-preserving SQL document and script splitter in `tsql-sql`.
- Added SQLite/Postgres execution in `tsql-db`.
- Added `config check`, `exec`, and `tui` commands in `tsql-app`.
- Added minimal Ratatui UI with Catppuccin Mocha theme in `tsql-tui`.
- Added SQLite integration test and Postgres CI integration job.
- Added examples and updated README/CHANGELOG.

## Local Verification

- `cargo fmt --all -- --check`: passed.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: passed.
- `cargo test --workspace --all-features`: passed.
- SQLite CLI smoke test: passed.
- `cargo audit`: passed with warnings from transitive Ratatui dependencies.
- Local Docker Compose Postgres: blocked by local Docker daemon error; CI service should verify Postgres.

## Next

- Finalize docs/automation knowledge artifacts.
- Re-run verification.
- Commit, push, and open PR.
